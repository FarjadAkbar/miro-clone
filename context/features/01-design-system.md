Read `AGENTS.md` before starting.
We're adding design system and UI components for the application.
install and configure shadcn ui for the application.

Add these component:
- Button
- Input
- Select
- Dialog
- Tooltip
- Badge
- Card
- Avatar
- DropdownMenu
- Separator
- Sheet
- Tabs
- Alert
- AlertDialog
- AlertDialogAction
- AlertDialogCancel
- AlertDialogContent
- AlertDialogDescription
- AlertDialogFooter
- AlertDialogHeader
- AlertDialogTitle
- AlertDialogTrigger

Don't modify the generate `components/ui/*` files after installation.
Also install lucide-react for icons.

Create lib/utils.ts file for cn function helper for merging Tailwind classes.
Ensure all components match the existing dark theme in globals.css.

# Check when done
- All components imports without error
- `cn()` function works correctly
- No default light styling applied
